<%= link_to({ controller: 'carts', action: 'list', production_id: model.id, **params.permit(:produce_on, :scene_id) }, class: 'weui-actionsheet__cell has-block-space-between') do %>
  <span class="is-several">
    <%= image_tag model.product.logo, class: 'image is-24x24' if model.product.logo.attached? %>
    <span><%= model.name %></span>
    <span class="has-text-danger"><%= model.price.to_money.format.html_safe %></span>
  </span>
  <span>
    <% if model.id == @production.id %>
      <i class="fa-solid fa-check has-text-success"></i>
    <% end %>
    <i class="fa-solid fa-chevron-right has-text-grey-light pl-1"></i>
  </span>
<% end %>
