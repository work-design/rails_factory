<%= form_with model: @item, url: { action: 'update_assign' }, method: :patch do |f| %>
  <%= render 'error_messages', target: f.object %>
  <%= f.collection_select :organ_id, @providers, :id, :name %>
  <%= f.submit %>
<% end %>
