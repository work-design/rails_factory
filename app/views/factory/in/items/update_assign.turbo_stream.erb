<%= turbo_stream.replace "cart_#{@item.cart_identity}", partial: 'purchase_cart_assign', locals: { cart: @item.current_cart, item: @item, item_con: controller_path } %>
<%= turbo_stream.remove 'modal' %>
