<%= f.fields :production_parts, model: @production.production_parts.build do |ef| %>

<tr data-controller="show" data-action="mouseenter->show#show mouseleave->show#hide">
  <%= yield %>
</tr>

<%= render partial: 'part_tbody', layout: 'part_tr', collection: component.parts, as: :part, locals: { f: ef } %>

<% end %>