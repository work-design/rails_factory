<tr id="tr_<%= model.id %>" data-controller="show" data-action="mouseenter->show#show mouseleave->show#hide">
  <%= yield %>
  <td>
    <div data-show-target="item" style="visibility: hidden">
      <%= link_to({ controller: 'produce_plans', scene_id: model.id }, class: 'button is-small is-rounded is-info') do %>
        <i class="fa-solid fa-hammer"></i>
        <span class="pl-1"><%= t('factory.admin.produce_plans.index.title') %></span>
      <% end %>
    </div>
  </td>
</tr>
