<%= render 'filter_form' %>
<%= render 'filter_table' %>
<%= render 'button' %>

<%= render layout: 'index_table' do %>
<% @product_hosts.group_by(&->(i){ i.product.product_taxon }).each do |taxon, products| %>

  <%= render partial: 'index_tbody', layout: 'index_tr', collection: products, as: :model %>
<% end %>
<% end %>

<%= paginate @product_hosts %>
