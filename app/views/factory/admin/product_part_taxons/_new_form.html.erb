<%= form_with model: @product_part_taxon, url: { action: 'create' } do |f| %>
  <%= render 'error_messages', target: f.object %>
  <%= f.collection_select :part_taxon_id, @part_taxons, :id, :name %>
  <%= f.number_field :min_select, step: 1 %>
  <%= f.submit %>
<% end %>
