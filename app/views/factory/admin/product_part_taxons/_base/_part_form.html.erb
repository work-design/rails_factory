<%= form_with theme: nil, model: product_part_taxon, url: { action: 'update', id: product_part_taxon.id }, data: { controller: 'input' } do |f| %>
  <%= f.collection_check_boxes :part_ids, product_part_taxon.part_taxon.parts, :id, :name, {}, { data: { action: 'input#form' } } %>
<% end %>
