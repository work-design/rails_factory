<%= form_with model: @provide, url: { action: 'create' }, class: 'modal-card' do |f| %>
  <header class="modal-card-head">
    <p class="modal-card-title"><%= t('.title') %></p>
    <button class="delete" data-action="modal#close"></button>
  </header>
  <section class="modal-card-body">
    <%= render 'error_messages', target: f.object  %>
    <%= render partial: 'provider_search', locals: { f: f } %>
  </section>
  <footer class="modal-card-foot is-block">
    <%= f.submit %>
  </footer>
<% end %>
