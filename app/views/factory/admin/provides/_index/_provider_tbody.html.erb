<td>
  <div class="is-several">
    <%= image_tag model.logo, class: 'image is-24x24' if model.logo.attached? %>
    <span><%= model.name %></span>
  </div>
</td>
<td></td>
<td>
  <%= button_to({ action: 'create' }, params: { 'provide[provider_id]': model.id }, class: 'button is-small is-rounded is-light') do %>
    <i class="fa-solid fa-plus color-success"></i>
    <span class="pl-1"><%= t('.create.title') %></span>
  <% end %>
</td>
