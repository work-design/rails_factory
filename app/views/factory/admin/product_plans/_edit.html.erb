<div class="ui modal" id="modal">
  <i class="close icon"></i>
  <div class="ui header">
    <%= t('.edit') %>
  </div>
  
  <div class="content">
    <%= form_with model: @product_plan, url: admin_product_plan_path(@product, @product_plan) do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
