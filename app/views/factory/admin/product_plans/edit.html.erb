<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_product_plans_path, class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Edit</div>
</div>

<div class="ui segment">
  <%= form_with model: @product, url: admin_product_plans_path(@product), local: true do |f| %>
    <%= render partial: 'form', locals: { f: f } %>
  <% end %>
</div>