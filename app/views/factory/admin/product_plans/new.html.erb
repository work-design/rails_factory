<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_product_plans_path, class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Add</div>
</div>

<div class="ui segment">
  <%= form_with model: @product_plan, url: admin_product_plans_path(@product), local: true do |f| %>
    <%= render partial: 'form', locals: { f: f } %>
  <% end %>
</div>