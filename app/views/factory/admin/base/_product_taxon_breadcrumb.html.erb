<li><%= link_to t('.product_taxons.index.title'), { controller: 'product_taxons' } %></li>
<li>
  <div class="navbar-item has-dropdown is-hoverable">
    <%= link_to @product_taxon.name, { controller: 'product_taxons' }, class: 'navbar-link' %>
    <div class="navbar-dropdown is-right">
      <% Factory::ProductTaxon.all.each do |taxon| %>
        <%= link_to taxon.name, { product_taxon_id: taxon.id }, class: 'navbar-item' %>
      <% end %>
    </div>
  </div>
</li>
