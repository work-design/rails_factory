<td><%= model.scene&.title %></td>
<td><%= model.produce_on %></td>
<td>
  <p><time data-controller="time"><%= model.start_at.to_formatted_s(:iso8601) if model.start_at %></time></p>
  <p><time data-controller="time"><%= model.finish_at.to_formatted_s(:iso8601) if model.finish_at %></time></p>
</td>
<td><%= model.state_i18n %></td>
<td>
  <p><%= model.planned_count %></p>
  <p><%= link_to model.production_items_count, { controller: 'production_items', production_plan_id: model.id } %></p>
</td>
