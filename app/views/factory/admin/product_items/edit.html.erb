<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_product_items_path, class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Edit</div>
</div>

<div class="ui segment">
  <%= form_with model: @product_item, url: admin_product_item_path(@product, @product_item), method: :patch, local: true do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
  <% end %>
</div>