<table class="ui very basic large table">
  <tbody>
    <tr>
      <td class="right aligned"><%= Product.human_attribute_name(:name) %></td>
      <td><%= @product.name %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= Product.human_attribute_name(:qr_prefix) %></td>
      <td><%= @product.qr_prefix %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= Product.human_attribute_name(:description) %></td>
      <td><%= @product.description %></td>
    </tr>
  </tbody>
</table>
