<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_products_path, class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Show</div>
</div>

<table class="ui very basic large table">
  <tbody>
    <tr>
      <td class="right aligned"><%= Product.human_attribute_name(:name) %></td>
      <td><%= @product.name %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= Product.human_attribute_name(:qr_code) %></td>
      <td><%= @product.qr_code %></td>
    </tr>
  </tbody>
</table>