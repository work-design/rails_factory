<%= form_with theme: 'search', model: Part.new do |f| %>
  <div class="field-body">
    <%= f.collection_select :organ_id, Organ.all, :id, :name, { include_blank: true } %>
    <%= f.text_field :name %>
    <div class="field is-narrow">
      <%= f.submit %>
    </div>
  </div>
<% end %>


