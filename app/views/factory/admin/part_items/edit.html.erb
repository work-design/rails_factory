<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_part_items_path, class: 'section' %>
  <div class="divider"> / </div>
  <div class="active section">Edit</div>
</div>

<div class="ui segment">
  <%= form_with model: @part_item, url: admin_part_item_path(@part, @part_item), method: :patch, local: true do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
  <% end %>
</div>