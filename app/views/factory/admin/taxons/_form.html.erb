<%= f.collection_select :parent_id, @taxons, :id, :name, include_blank: true if @taxons.present? %>
<%= f.text_field :name, autofocus: true %>
<%= f.file_field :logo %>
<%= f.file_field :logo_color %>
<%= f.collection_select :scene_id, @scenes, :id, :title if @scenes.present? %>
