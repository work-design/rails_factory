<%= render 'top_bar' %>

<%= render layout: 'index_list' do %>
  <% @products.group_by(&:taxon).each do |taxon, products| %>
    <%= render partial: 'index_item', layout: 'index_item_actions', collection: products, as: :model %>
  <% end %>
<% end %>

<%= paginate @products %>
