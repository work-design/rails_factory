<%= f.collection_select :parent_id, @product_taxons, :id, :name, include_blank: true if @product_taxons.present? %>
<%= f.text_field :name %>
<%= f.file_field :logo %>
<%= f.file_field :logo_color %>
<%= f.collection_select :scene_id, @scenes, :id, :title if @scenes.present? %>
