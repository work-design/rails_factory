<td>
  <div class="is-several">
    <%= image_tag model.logo, class: 'image is-24x24' if model.logo.attached? %>
    <span><%= model.name %></span>
  </div>
</td>
<td><%= model.scene&.title %></td>
<td>
  <%= link_to({ controller: 'factory_providers', factory_taxon_id: model.id }, aria: { label: t('factory.panel.factory_providers.index.title') }, class: 'button is-small is-rounded is-light') do %>
    <i class="fa-solid fa-industry color-info"></i>
    <span class="pl-1"><%= model.factory_providers_count %></span>
  <% end %>
</td>
