<tr>
  <td><%= part_taxon_provider.provider.name %></td>
  <td>
    <%= link_to({ action: 'destroy', id: part_taxon_provider.id }, { method: :delete, aria: { label: t('.destroy') }, data: { confirm: t('.confirm') }, remote: true, class: 'button is-small is-rounded is-light' }) do %>
      <i class="fas fa-trash-alt"></i>
    <% end %>
  </td>
</tr>
