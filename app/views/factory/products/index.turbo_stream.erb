<%= turbo_stream.replace 'container' do %>
  <%= render partial: 'product', collection: @products, as: :model %>
  <%= render 'loading' %>
<% end %>
<%= turbo_stream.replace 'tip', partial: 'no_more' if @products.total_pages == @products.current_page %>
