<%= render 'breadcrumb' %>

<div class="weui-flex">
  <%= render 'menu' %>
  <div class="weui-panel weui-panel_access mt-0 weui-flex__item">
    <div class="weui-panel__bd">
      <%= render partial: 'product', collection: @products, as: :model %>
    </div>
  
    <%= paginate @products %>
  </div>
</div>
<%= render 'taxon' %>
