<%= form_with model: production, url: { action: 'create_dialog' }, id: 'edit_custom' do |f| %>
  <%= hidden_field_tag :dispatch, params[:dispatch] %>
<% end %>
<%# render 'factory/productions/_base/figure' if production.product.covers.length > 0 %>

<% production.same_product_parts.includes(:taxon_component, :part).group_by(&:taxon_component).each do |taxon_component, product_parts| %>
  <%= render partial: 'taxon_component', locals: { taxon_component: taxon_component, product_parts: product_parts } %>
<% end %>
